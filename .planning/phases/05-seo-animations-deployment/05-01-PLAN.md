---
phase: 05-seo-animations-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/contact/layout.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/not-found.tsx
  - public/og.png
autonomous: true
requirements:
  - SEOP-01
  - SEOP-02
  - SEOP-03
  - SEOP-04

must_haves:
  truths:
    - "Sharing mokshverma.in on LinkedIn/Slack/Twitter shows a rich preview with OG image, title, and description"
    - "Visiting /sitemap.xml returns a valid XML sitemap with both pages"
    - "Visiting /robots.txt returns a valid robots file allowing all crawlers"
    - "Visiting a non-existent URL shows a branded 404 page with a link back to home"
    - "Each page has unique title and description meta tags"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Extended metadata with metadataBase, openGraph, twitter fields, title template"
      contains: "metadataBase"
    - path: "src/app/contact/layout.tsx"
      provides: "Contact-specific OG metadata with different title/description"
      contains: "openGraph"
    - path: "src/app/sitemap.ts"
      provides: "Dynamic sitemap generation for /sitemap.xml"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Dynamic robots.txt generation for /robots.txt"
      exports: ["default"]
    - path: "src/app/not-found.tsx"
      provides: "Custom 404 page matching site design"
      exports: ["default"]
    - path: "public/og.png"
      provides: "Static OG image for social sharing previews"
  key_links:
    - from: "src/app/layout.tsx"
      to: "public/og.png"
      via: "openGraph.images url field"
      pattern: 'url:.*"/og.png"'
    - from: "src/app/sitemap.ts"
      to: "https://mokshverma.in"
      via: "sitemap URL entries"
      pattern: "mokshverma\\.in"
    - from: "src/app/robots.ts"
      to: "src/app/sitemap.ts"
      via: "sitemap field reference"
      pattern: "sitemap.*mokshverma\\.in/sitemap\\.xml"
---

<objective>
Add SEO metadata, OG image, sitemap, robots.txt, and a custom 404 page so the site is discoverable by search engines and shows rich previews when shared on social platforms.

Purpose: Without proper metadata, search engines cannot index the site effectively and social shares show generic link previews instead of a branded card with Moksh's name and title.
Output: Extended metadata on both pages, static OG image in public/, sitemap.ts, robots.ts, not-found.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-animations-deployment/05-CONTEXT.md
@.planning/phases/05-seo-animations-deployment/05-RESEARCH.md

<interfaces>
<!-- Current layout.tsx metadata export that must be extended (not replaced) -->
From src/app/layout.tsx:
```typescript
export const metadata: Metadata = {
  title: "Moksh Verma | Backend Engineer",
  description:
    "Backend Engineer with 5+ years of experience building distributed systems at scale.",
};
```

From src/app/contact/layout.tsx:
```typescript
export const metadata: Metadata = {
  title: "Contact | Moksh Verma",
  description: "Get in touch with Moksh Verma - Backend Engineer",
};
```

<!-- Design tokens from globals.css for 404 page styling -->
Key CSS tokens: bg-background (#0a0a0a), text-foreground (#ededed), text-muted (#a1a1aa), bg-accent (#3b82f6), text-hero (3rem), text-body (1rem), text-sm (0.875rem)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEO metadata, sitemap, robots, and 404 page</name>
  <files>src/app/layout.tsx, src/app/contact/layout.tsx, src/app/sitemap.ts, src/app/robots.ts, src/app/not-found.tsx</files>
  <read_first>src/app/layout.tsx, src/app/contact/layout.tsx, src/app/globals.css, .planning/phases/05-seo-animations-deployment/05-RESEARCH.md</read_first>
  <action>
**1. Extend root layout metadata (src/app/layout.tsx)** per D-01, D-02 (SEOP-01, SEOP-02):

Replace the existing `metadata` export with:
```typescript
export const metadata: Metadata = {
  metadataBase: new URL("https://mokshverma.in"),
  title: {
    default: "Moksh Verma | Backend Engineer",
    template: "%s | Moksh Verma",
  },
  description:
    "Backend Engineer building scalable systems. 5+ years at Expedia, Gaana, Radio Mirchi.",
  openGraph: {
    type: "website",
    url: "https://mokshverma.in",
    title: "Moksh Verma | Backend Engineer",
    description:
      "Backend Engineer building scalable systems. 5+ years at Expedia, Gaana, Radio Mirchi.",
    siteName: "Moksh Verma",
    images: [
      {
        url: "/og.png",
        width: 1200,
        height: 630,
        alt: "Moksh Verma - Backend Engineer",
      },
    ],
  },
  twitter: {
    card: "summary_large_image",
    title: "Moksh Verma | Backend Engineer",
    description:
      "Backend Engineer building scalable systems. 5+ years at Expedia, Gaana, Radio Mirchi.",
    images: ["/og.png"],
  },
};
```

Do NOT change any other part of layout.tsx (font loading, body structure, Navigation, Footer, etc.). Only the `metadata` export changes.

**2. Extend contact layout metadata (src/app/contact/layout.tsx)** per D-03 (SEOP-01):

Replace the existing `metadata` export with:
```typescript
export const metadata: Metadata = {
  title: "Contact",
  description: "Get in touch with Moksh Verma.",
  openGraph: {
    title: "Contact | Moksh Verma",
    description: "Get in touch with Moksh Verma.",
  },
};
```

The title "Contact" will become "Contact | Moksh Verma" via the root layout's `title.template`. OG images are inherited from root layout.

**3. Create sitemap.ts (src/app/sitemap.ts)** (SEOP-03):

```typescript
import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: "https://mokshverma.in",
      lastModified: new Date(),
      changeFrequency: "monthly",
      priority: 1,
    },
    {
      url: "https://mokshverma.in/contact",
      lastModified: new Date(),
      changeFrequency: "monthly",
      priority: 0.8,
    },
  ];
}
```

**4. Create robots.ts (src/app/robots.ts)** (SEOP-03):

```typescript
import type { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: "*",
      allow: "/",
    },
    sitemap: "https://mokshverma.in/sitemap.xml",
  };
}
```

**5. Create custom 404 page (src/app/not-found.tsx)** per D-08 (SEOP-04):

Dark background, on-brand with the site aesthetic, with a home link. Use existing design tokens (text-hero for "404", text-muted for subtitle, bg-accent for the CTA button). Import `Link` from `next/link`. Use `FiArrowLeft` from `react-icons/fi` for the back arrow icon.

```typescript
import Link from "next/link";
import { FiArrowLeft } from "react-icons/fi";

export default function NotFound() {
  return (
    <section className="flex min-h-[80vh] flex-col items-center justify-center px-6 text-center">
      <h1 className="text-hero font-bold leading-hero text-foreground">404</h1>
      <p className="mt-4 text-body text-muted">
        This page doesn&apos;t exist.
      </p>
      <Link
        href="/"
        className="mt-8 inline-flex items-center gap-2 rounded-lg bg-accent px-6 py-3 text-sm font-bold text-background transition-colors hover:bg-accent-hover"
      >
        <FiArrowLeft className="h-4 w-4" />
        Back to Home
      </Link>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/mokverma/Moksh/Personal/resume-website && npx next build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
- grep -q "metadataBase" src/app/layout.tsx
- grep -q "openGraph" src/app/layout.tsx
- grep -q "twitter" src/app/layout.tsx
- grep -q "/og.png" src/app/layout.tsx
- grep -q "title.template\|template.*Moksh" src/app/layout.tsx (title template pattern present)
- grep -q "openGraph" src/app/contact/layout.tsx
- grep -q "Get in touch with Moksh Verma" src/app/contact/layout.tsx
- test -f src/app/sitemap.ts
- grep -q "mokshverma.in" src/app/sitemap.ts
- grep -q "MetadataRoute.Sitemap" src/app/sitemap.ts
- test -f src/app/robots.ts
- grep -q "mokshverma.in/sitemap.xml" src/app/robots.ts
- test -f src/app/not-found.tsx
- grep -q "404" src/app/not-found.tsx
- grep -q 'href="/"' src/app/not-found.tsx
- npx next build succeeds without errors
  </acceptance_criteria>
  <done>Root layout has complete OG/Twitter metadata with metadataBase and title template. Contact layout has OG metadata per D-03. sitemap.ts serves /sitemap.xml with both pages. robots.ts serves /robots.txt allowing all crawlers. not-found.tsx renders a branded 404 page with home link.</done>
</task>

<task type="auto">
  <name>Task 2: Create static OG image</name>
  <files>public/og.png</files>
  <read_first>src/app/globals.css, .planning/phases/05-seo-animations-deployment/05-CONTEXT.md</read_first>
  <action>
Create a static OG image at `public/og.png` (1200x630 pixels) per D-01.

The image must be a PNG file, exactly 1200x630 pixels, with:
- Dark background (#0a0a0a)
- Name "Moksh Verma" in large white text (#ededed), centered
- Title "Backend Engineer" below the name in accent blue (#3b82f6)
- Clean, minimal design -- no photos, no complex graphics

Use a Node.js script with the `canvas` package (npx approach) to generate this image programmatically. Run:

```bash
npx -y canvas-sketch-cli --help 2>/dev/null || true
```

If canvas is not easily available, use an alternative approach: create the OG image using Next.js `ImageResponse` from `next/og` as a one-time generation script, or use a simple HTML-to-image approach with a temp script.

**Simplest approach:** Create a temporary Node.js script that uses the `@vercel/og` or `sharp` package to generate the PNG. Or even simpler -- use the `satori` + `sharp` approach:

```bash
# Generate OG image using a temp script
node -e "
const { createCanvas } = require('canvas');
// ... generate image
"
```

If `canvas` npm package is not available, try this alternative:
1. Create a temporary HTML file with the OG design
2. Use `npx capture-website-cli` or similar to screenshot it

**Fallback approach (most reliable):** Create an SVG-based approach and convert to PNG using a locally available tool, or simply create the file using ImageMagick if available:

```bash
convert -size 1200x630 xc:'#0a0a0a' \
  -font Helvetica -pointsize 72 -fill '#ededed' -gravity center -annotate +0-40 'Moksh Verma' \
  -font Helvetica -pointsize 36 -fill '#3b82f6' -gravity center -annotate +0+40 'Backend Engineer' \
  public/og.png
```

The executor should try the most straightforward approach available on the system. The key constraint is: the resulting file must be a PNG at 1200x630, exist at `public/og.png`, and look professional (dark background, white name, blue title).
  </action>
  <verify>
    <automated>test -f /Users/mokverma/Moksh/Personal/resume-website/public/og.png && file /Users/mokverma/Moksh/Personal/resume-website/public/og.png | grep -q "PNG" && echo "OG image exists and is PNG"</automated>
  </verify>
  <acceptance_criteria>
- test -f public/og.png
- file public/og.png | grep -q "PNG"
- The file is approximately 1200x630 pixels (verify with: identify public/og.png or file public/og.png shows dimensions)
  </acceptance_criteria>
  <done>A static OG image exists at public/og.png, is a valid PNG file at 1200x630 dimensions, and displays Moksh Verma's name and title on a dark background with accent blue.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes successfully
2. Build output includes /sitemap.xml and /robots.txt routes
3. Build output includes /_not-found page
4. `public/og.png` exists and is a valid PNG
5. `grep -q "metadataBase" src/app/layout.tsx` passes
6. `grep -q "openGraph" src/app/contact/layout.tsx` passes
</verification>

<success_criteria>
- Both pages have complete meta tags with OG and Twitter card metadata
- /sitemap.xml and /robots.txt are generated at build time
- Non-existent routes render the custom 404 page
- OG image is a valid 1200x630 PNG in public/
- `npx next build` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-animations-deployment/05-01-SUMMARY.md`
</output>
