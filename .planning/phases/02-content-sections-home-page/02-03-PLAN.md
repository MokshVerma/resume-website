---
phase: 02-content-sections-home-page
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/app/_components/footer.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: false
requirements: [FOOT-01, FOOT-02]

must_haves:
  truths:
    - "Footer displays GitHub, LinkedIn, and email social links with icons"
    - "Footer includes a downloadable resume PDF link"
    - "Footer appears on every page (rendered in layout.tsx, not page.tsx)"
    - "Home page shows all sections in correct order: Hero, Experience, Skills, Projects, Education"
    - "Page builds successfully and all sections render from real data"
  artifacts:
    - path: "src/app/_components/footer.tsx"
      provides: "Footer with social links and resume download"
      exports: ["Footer"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with Footer component inside <footer> landmark"
      contains: "<Footer"
    - path: "src/app/page.tsx"
      provides: "Complete home page assembling all section components"
      contains: "HeroSection"
  key_links:
    - from: "src/app/_components/footer.tsx"
      to: "@/lib/data"
      via: "import { personalInfo }"
      pattern: "import.*personalInfo.*from.*@/lib/data"
    - from: "src/app/_components/footer.tsx"
      to: "react-icons/fi"
      via: "import { FiGithub, FiLinkedin, FiMail }"
      pattern: "import.*from.*react-icons/fi"
    - from: "src/app/layout.tsx"
      to: "src/app/_components/footer.tsx"
      via: "import { Footer }"
      pattern: "import.*Footer.*from.*_components/footer"
    - from: "src/app/page.tsx"
      to: "src/app/_components/hero-section.tsx"
      via: "import { HeroSection }"
      pattern: "import.*HeroSection.*from.*_components/hero-section"
    - from: "src/app/page.tsx"
      to: "src/app/_components/experience-section.tsx"
      via: "import { ExperienceSection }"
      pattern: "import.*ExperienceSection"
---

<objective>
Create the footer component, add it to the root layout, and assemble all section components into the home page. This is the final wiring plan that produces the complete, viewable resume page.

Purpose: Wire all sections together so the site displays a complete resume at the root URL. Footer goes in layout.tsx (not page.tsx) so it appears on all pages including future /contact.
Output: Footer component, updated layout.tsx with Footer, updated page.tsx with all sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections-home-page/02-CONTEXT.md
@.planning/phases/02-content-sections-home-page/02-RESEARCH.md
@.planning/phases/02-content-sections-home-page/02-UI-SPEC.md
@src/lib/data.ts
@src/app/globals.css
@src/app/layout.tsx
@src/app/page.tsx

<!-- Plan 01 and 02 SUMMARYs needed for component import paths and export names -->
@.planning/phases/02-content-sections-home-page/02-01-SUMMARY.md
@.planning/phases/02-content-sections-home-page/02-02-SUMMARY.md

<interfaces>
<!-- Components created in Plans 01 and 02 that this plan wires together -->

From Plan 01 (src/app/_components/):
```typescript
// hero-section.tsx - Server Component
export function HeroSection(): JSX.Element;

// experience-section.tsx - Server Component
export function ExperienceSection(): JSX.Element;
```

From Plan 02 (src/app/_components/):
```typescript
// skills-section.tsx - Server Component
export function SkillsSection(): JSX.Element;

// projects-section.tsx - Server Component
export function ProjectsSection(): JSX.Element;

// education-section.tsx - Server Component
export function EducationSection(): JSX.Element;
```

From src/lib/data.ts:
```typescript
export const personalInfo: PersonalInfo = {
  name: "Moksh Verma",
  email: "mokshverma98@gmail.com",
  linkedin: "https://www.linkedin.com/in/moksh-verma",
  github: "https://github.com/MokshVerma",
  // ...
};
```

From src/app/layout.tsx (current state):
```typescript
<footer role="contentinfo">
  {/* Footer -- built in Phase 2 */}
</footer>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Footer component and wire into layout.tsx, assemble page.tsx with all sections</name>
  <files>
    src/app/_components/footer.tsx
    src/app/layout.tsx
    src/app/page.tsx
  </files>
  <read_first>
    src/lib/data.ts
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    src/app/_components/hero-section.tsx
    src/app/_components/experience-section.tsx
    src/app/_components/skills-section.tsx
    src/app/_components/projects-section.tsx
    src/app/_components/education-section.tsx
    .planning/phases/02-content-sections-home-page/02-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Create Footer component** at `src/app/_components/footer.tsx`:
- Server Component (no "use client")
- Import `personalInfo` from `@/lib/data`
- Import `FiGithub`, `FiLinkedin`, `FiMail` from `react-icons/fi` (per UI-SPEC: Feather Icons, 20px size)
- Per D-16 (social links), D-17 (resume PDF link), D-18 (minimal):

Layout per UI-SPEC Footer spec:
```
<div className="bg-surface py-8 md:py-12 px-6 md:px-12 lg:px-24">
  <div className="mx-auto max-w-4xl text-center">
```

Social links row per UI-SPEC:
```
<div className="flex items-center justify-center gap-6">
  <a href={personalInfo.github} target="_blank" rel="noopener noreferrer"
     aria-label="GitHub profile"
     className="text-muted hover:text-foreground transition-colors">
    <FiGithub className="h-5 w-5" />
  </a>
  <a href={personalInfo.linkedin} target="_blank" rel="noopener noreferrer"
     aria-label="LinkedIn profile"
     className="text-muted hover:text-foreground transition-colors">
    <FiLinkedin className="h-5 w-5" />
  </a>
  <a href={`mailto:${personalInfo.email}`}
     aria-label="Send email"
     className="text-muted hover:text-foreground transition-colors">
    <FiMail className="h-5 w-5" />
  </a>
</div>
```

Resume download link per FOOT-02 and UI-SPEC:
```
<a href="/resume.pdf"
   aria-label="Download resume as PDF"
   className="mt-4 inline-block text-sm text-muted hover:text-accent transition-colors underline">
  Download Resume (PDF)
</a>
```

Copyright line per UI-SPEC:
```
<p className="mt-4 text-sm text-muted">&copy; Moksh Verma</p>
```

- All external links (GitHub, LinkedIn) use `target="_blank" rel="noopener noreferrer"`
- Email uses `mailto:` link (same tab, no target blank)
- Resume PDF link opens in same tab (download behavior)
- Icon size: `h-5 w-5` (20px per UI-SPEC)
- Export as named export `Footer`

**Step 2: Update layout.tsx** to render Footer inside existing `<footer>` landmark:
- Import `Footer` from `./_components/footer`
- Replace the placeholder comment `{/* Footer -- built in Phase 2 */}` with `<Footer />`
- Keep ALL other existing content in layout.tsx unchanged (metadata, fonts, header placeholder, main)
- CRITICAL: Footer goes in layout.tsx NOT page.tsx (RESEARCH.md Pitfall 5). This ensures the footer appears on all pages including future /contact.

**Step 3: Replace page.tsx** with full section assembly:
- Remove the entire current placeholder content
- Import all section components from `./_components/`:
  - `HeroSection` from `./_components/hero-section`
  - `ExperienceSection` from `./_components/experience-section`
  - `SkillsSection` from `./_components/skills-section`
  - `ProjectsSection` from `./_components/projects-section`
  - `EducationSection` from `./_components/education-section`
- Per D-05, sections in this order: Hero -> Experience -> Skills -> Projects -> Education
- Per RESEARCH.md Page Assembly pattern:
```typescript
export default function HomePage() {
  return (
    <>
      <HeroSection />
      <ExperienceSection />
      <SkillsSection />
      <ProjectsSection />
      <EducationSection />
    </>
  );
}
```
- Do NOT include Footer here (it is in layout.tsx)
- Do NOT add "use client" (page is a Server Component)
- Remove the `import { personalInfo }` that the old placeholder used (no longer needed in page.tsx)
  </action>
  <verify>
    <automated>cd /Users/mokverma/Moksh/Personal/resume-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'FiGithub' src/app/_components/footer.tsx (GitHub icon from react-icons/fi)
    - grep -q 'FiLinkedin' src/app/_components/footer.tsx (LinkedIn icon)
    - grep -q 'FiMail' src/app/_components/footer.tsx (Email icon)
    - grep -q 'personalInfo' src/app/_components/footer.tsx (imports data from lib/data.ts)
    - grep -q '/resume.pdf' src/app/_components/footer.tsx (resume PDF download link per FOOT-02)
    - grep -q 'aria-label' src/app/_components/footer.tsx (accessible link labels)
    - grep -q 'Footer' src/app/layout.tsx (Footer component rendered in layout)
    - grep -q 'HeroSection' src/app/page.tsx (hero section imported and rendered)
    - grep -q 'ExperienceSection' src/app/page.tsx (experience section rendered)
    - grep -q 'SkillsSection' src/app/page.tsx (skills section rendered)
    - grep -q 'ProjectsSection' src/app/page.tsx (projects section rendered)
    - grep -q 'EducationSection' src/app/page.tsx (education section rendered)
    - grep -rL 'Footer' src/app/page.tsx (Footer is NOT in page.tsx -- it is in layout.tsx)
  </acceptance_criteria>
  <done>
    - Footer displays GitHub, LinkedIn, and email icons as social links with correct URLs from personalInfo
    - Footer has "Download Resume (PDF)" link pointing to /resume.pdf
    - Footer renders inside the <footer> landmark in layout.tsx, not in page.tsx
    - Home page (page.tsx) renders all 5 sections in correct order: Hero, Experience, Skills, Projects, Education
    - All social links have aria-labels for accessibility
    - External links use target="_blank" rel="noopener noreferrer"
    - npm run build succeeds with zero errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete resume page</name>
  <files>src/app/page.tsx</files>
  <action>
CHECKPOINT: Human visual verification of the complete resume page built across all 3 plans.

What was built:
1. Hero section with name, title, greeting, summary, and two CTAs
2. Experience timeline with 4 roles and highlighted impact metrics
3. Skills grid with 6 categories of chips/tags
4. Projects grid with 2 glassmorphism cards and hover effects
5. Education section with degree and university
6. Footer with social icons and resume download link

Verification steps:
1. Run `npm run dev` and open http://localhost:3000 in a browser
2. Hero section (top): Verify time-based greeting shows (e.g., "Good afternoon, I'm"), "Moksh Verma" displays large and bold, "Backend Engineer" title appears below name, summary paragraph is visible. Click "Download Resume" -- should open/download resume.pdf. Click "Get in Touch" -- should navigate to /contact (will 404, that is expected).
3. Experience section: Verify all 4 roles visible (Expedia Group, Radio Mirchi, Gaana, EPAM Systems). Verify accent-colored numbers (380M+, $200K+, etc.) in bullet points. Verify timeline dots/line visible on desktop.
4. Skills section: Verify 6 category groups with chip-styled skills. Verify no percentage bars anywhere.
5. Projects section: Hover over a project card -- should lift with accent glow shadow. Verify glassmorphism effect (semi-transparent, slightly blurred background). Verify GitHub links present with icons.
6. Education section: Verify degree, university, and period displayed.
7. Footer (bottom): Verify GitHub, LinkedIn, email icons visible. Verify "Download Resume (PDF)" link present. Hover over icons -- should transition from muted to foreground color.
8. Resize browser to mobile width (~375px): Verify hero CTAs stack vertically, skills grid collapses to single column, projects grid collapses to single column, experience cards stack without timeline line.

Resume signal: Type "approved" to confirm the page looks correct, or describe any visual issues to fix.
  </action>
  <verify>
    <automated>cd /Users/mokverma/Moksh/Personal/resume-website && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Human has visually verified the complete resume page and approved all sections render correctly on both desktop and mobile viewports</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` passes with zero errors
2. Footer component exists at src/app/_components/footer.tsx
3. layout.tsx imports and renders Footer inside <footer> landmark
4. page.tsx imports and renders all 5 section components in order
5. No "use client" in page.tsx, layout.tsx, or footer.tsx
6. Visual verification confirms complete resume page with all sections
</verification>

<success_criteria>
- Complete, visually polished resume page viewable at http://localhost:3000
- All 14 Phase 2 requirements (HERO-01..03, EXPR-01..03, SKLL-01..02, PROJ-01..03, EDUC-01, FOOT-01..02) implemented
- Footer appears in layout.tsx, available on all pages
- Page sections follow correct order: Hero -> Experience -> Skills -> Projects -> Education -> Footer
- Human has visually verified the page and approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections-home-page/02-03-SUMMARY.md`
</output>
